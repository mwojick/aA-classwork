class Game {
  constructor(player) {
    this.player = player;
    this.towers = [[1,2,3],[],[]];
  }
  
  run() {
      // until player wins
      // show tower
      // prompt user for from/to index
      // check if valid move?
      // make move
  }
  
  promptMove() {
    // show tower
    // prompt user for from/to index
  }
  
  isValidMove(start,end) {
    
  }
  
  move(sart,end){
    
  }
  
}
